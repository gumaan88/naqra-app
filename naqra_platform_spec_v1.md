# نقرأ
## وثيقة المتطلبات والمواصفات الوظيفية والفنية
الإصدار: 1.0 — 17 سبتمبر 2026

**فكرة المنصة:** منصة ويب عربية تعليمية للأطفال تساعد الطفل على قراءة الكلمات والتعرف على حروفها وربط الكلمات بالصور، مع ملف مستقل لكل طفل، وتتبع تطوره في الخلفية، ولوحة مبسطة لولي الأمر، وبنية قابلة لإضافة ألعاب تعليمية جديدة لاحقًا.

**المكدس التقني المستهدف:** React + TypeScript + Cloudflare Pages/Workers + D1 + R2 + Workers AI، مع تصميم Local-first ومزامنة مجمعة لتقليل اتصالات الـ Worker.

---

# 1. المبادئ الملزمة للمشروع
- واجهة الطفل عربية RTL، بسيطة جدًا، واضحة، كبيرة اللمس، وخالية من عناصر الإدارة أو المصطلحات التقنية.
- لا يتم استدعاء الـ Worker مع كل ضغطة. اللعب يعمل محليًا، والأحداث تجمع في IndexedDB ثم ترسل على دفعة واحدة في نهاية الجلسة، مع آلية احتياطية لمنع فقد البيانات.
- لا يتم توليد الكلمات أو الصور أثناء لعب الطفل. كل المحتوى يولد مسبقًا ويخزن ويعتمد قبل عرضه.
- المحتوى الجديد من الذكاء الاصطناعي لا يدخل اللعب مباشرة؛ يمر بالتحقق الآلي ثم الاعتماد.
- كل طفل يملك تقدمه ونتائجه ومستواه وإحصاءاته بصورة مستقلة.
- السرعة تقاس للتحليل، ولا تستخدم كعامل ضغط على الطفل في النسخة الأولى.
- لا توجد مقارنة بين الإخوة أو لوحة ترتيب افتراضية؛ التركيز على تقدم الطفل مقارنة بأدائه السابق.

# 2. المستخدمون والصلاحيات
## 2.1 ولي الأمر
يسجل حسابًا مستقلًا بالبريد وكلمة المرور، ويضيف أكثر من طفل، ويعدل بياناتهم وصورهم، ويشاهد الإحصاءات والتقارير، ويحدد إعدادات الصوت والمستوى ويستطيع الدخول إلى واجهة الطفل.

## 2.2 الطفل
يدخل بطريقة مبسطة من جهاز العائلة الموثوق: اختيار صورته/اسمه ثم رقم شخصي قصير إن كان مفعّلًا. لا يحتاج بريدًا ولا كلمة مرور ولا يستطيع الوصول إلى بيانات ولي الأمر.

## 2.3 مدير النظام
يدير بنك الكلمات والصور والمستويات، يشغل دفعات التوليد بالذكاء الاصطناعي، يراجع المحتوى ويعتمده أو يرفضه، ويراقب الأخطاء العامة. هذه الواجهة منفصلة تمامًا عن حساب ولي الأمر.

# 3. الهوية وتجربة الاستخدام
الاسم: **نقرأ**.

الهوية المقترحة: شعار كتاب مفتوح بسيط مع حرف عربي واضح، وحواف مستديرة ورسوم خفيفة. أسلوب بصري طفولي محترم، مبهج لكن غير مزدحم.

الألوان:
- أساسي تركوازي: #24A9A0
- أصفر مكافآت: #FFC857
- مرجاني مساعد: #FF7A6B
- نجاح: #34A853
- خطأ: #E55353
- خلفية دافئة: #FFF9F0
- نص رئيسي: #24303A
- بنفسجي للوحة ولي الأمر: #7357C8

الخط: Noto Sans Arabic أو خط عربي واضح مماثل. الكلمة المستهدفة كبيرة جدًا (تقريبًا 56–72px على الهاتف)، وأزرار الطفل لا تقل عن 56px ارتفاعًا/عرضًا. الحركة قصيرة 150–300ms والاحتفال لا يتجاوز 1–1.5 ثانية حتى لا يعطل اللعب.

# 4. التسجيل والدخول وإدارة الأطفال
## 4.1 تسجيل ولي الأمر
الحقول: الاسم، البريد الإلكتروني، كلمة المرور، تأكيد كلمة المرور. يتم إنشاء جلسة آمنة عبر Cookie من نوع HttpOnly/Secure/SameSite.

## 4.2 إضافة طفل
الحد الأدنى من البيانات: الاسم المعروض، العمر أو سنة الميلاد، الجنس اختياري، صورة اختيارية، المستوى الابتدائي، رقم طفل محلي قصير يختاره ولي الأمر. لا يلزم الاسم الكامل أو تاريخ الميلاد الكامل.

## 4.3 دخول الطفل
بعد اعتماد الجهاز لعائلة محددة تظهر بطاقات الأطفال بصور كبيرة. يضغط الطفل على صورته ثم يدخل رقمه إن كان القفل مفعّلًا. الرقم فريد داخل حساب ولي الأمر فقط، وليس معرفًا عامًا يمكن البحث به.

# 5. شاشة الطفل الرئيسية
تظهر صورة الطفل واسمه وترحيب قصير ونقاطه الإجمالية، ثم بطاقات الألعاب. في النسخة الأولى:
1. **حروف الكلمة — اقرأ الكلمة وحدد حروفها بالترتيب.**
2. **الكلمة والصورة — اقرأ الكلمة واختر الصورة المطابقة.**

لا تظهر رسوم بيانية أو نسب أو أخطاء تفصيلية للطفل. بعد كل جلسة يظهر ملخص إيجابي قصير مثل: عدد الكلمات المكتملة والنقاط والنجوم.

# 6. اللعبة الأولى: حروف الكلمة
## 6.1 الهدف
مساعدة الطفل على قراءة كلمة عربية ظاهرة أمامه، ثم التعرف على حروفها واختيارها بالترتيب الصحيح من بين الحروف الصحيحة وحروف مشتتة.

## 6.2 تكوين الشاشة
النصف العلوي: الكلمة العربية كاملة من دون تشكيل، بخط كبير وواضح على خلفية هادئة.

أسفل الكلمة: شريط تقدم اختياري يحتوي خانات بعدد حروف الكلمة؛ كل حرف صحيح يملأ الخانة المقابلة.

النصف السفلي: بطاقات حروف كبيرة قابلة للضغط. تحتوي على كل حروف الكلمة بعدد مرات ظهورها فعليًا، مضافًا إليها حروف مشتتة، ثم يتم خلطها محليًا.

مثال: الكلمة «أبي». يجب أن تكون البطاقات مثل: أ، ب، ي، م، هـ، ت بترتيب عشوائي.

## 6.3 قواعد إنشاء الحروف المشتتة
- كل ظهور لحرف متكرر في الكلمة يحتاج بطاقة مستقلة؛ مثال «باب» يحتاج ب + ا + ب.
- المستوى المبتدئ يستخدم مشتتات مختلفة بوضوح عن الحرف المطلوب، ولا يكرر حرفًا من الكلمة كحرف زائد إلا إذا كان التكرار جزءًا حقيقيًا من الكلمة.
- المستويات المتقدمة يمكن أن تستخدم حروفًا متشابهة بصريًا لرفع التحدي مثل ب/ت/ث أو ج/ح/خ.
- عدد البطاقات المقترح: كلمات 2–3 حروف = 5–6 بطاقات، 4 حروف = 7 بطاقات تقريبًا، 5–6 حروف = 8–10 بطاقات. الحد الأقصى يظل مناسبًا للشاشة.
- لا تستخدم أرقام أو رموز أو تشكيل داخل بطاقات الحروف.

## 6.4 منطق الضغط الصحيح والخاطئ — ملزم
يحتفظ العميل بمؤشر `expectedIndex` يبدأ من صفر ويشير إلى الحرف المطلوب حاليًا.

عند ضغط بطاقة:
1. إذا كانت البطاقة مستخدمة مسبقًا: يتم تجاهل الضغط دون تسجيل خطأ.
2. إذا كان حرف البطاقة يساوي حرف الكلمة في `expectedIndex`: الضغط صحيح. تتحول البطاقة إلى أخضر، تثبت ولا يمكن استخدامها مرة أخرى، تملأ خانة التقدم، يزداد `expectedIndex` بمقدار واحد، ويصفّر عداد الأخطاء المتتالية للحرف الحالي.
3. إذا كان الحرف موجودًا في الكلمة لكنه ليس الحرف المطلوب الآن: يعتبر خطأ ترتيب. تومض البطاقة بالأحمر وتهتز اهتزازًا خفيفًا ويصدر صوت خطأ قصير، لكنها تبقى متاحة ليستخدمها الطفل لاحقًا عندما يحين دورها.
4. إذا كان الحرف مشتتًا ولا ينتمي للكلمة: نفس معالجة الخطأ السابقة.
5. لا يتغير `expectedIndex` عند أي خطأ.

## 6.5 نظام المساعدة بعد الأخطاء
لكل موضع في الكلمة عداد `consecutiveErrorsForCurrentLetter`.
- بعد 3 أخطاء متتالية قبل اختيار الحرف الحالي: تضيء/تنبض إحدى البطاقات الصحيحة المتاحة لمدة 1–1.5 ثانية بلون أصفر خفيف، من دون اختيارها تلقائيًا.
- تسجل المساعدة في الإحصاءات `hintUsed = true` ويزداد `hintCount`.
- عند اختيار الحرف الصحيح يصفر عداد الأخطاء لذلك الموضع.
- لا تعرض الحل كاملًا تلقائيًا في النسخة الأولى.

## 6.6 الصوت والحركة
- صحيح: نغمة نجاح قصيرة وهادئة.
- خطأ: صوت تنبيه بسيط غير مزعج.
- إكمال الكلمة: تصفيق قصير + بالونات/قصاصات احتفالية لمدة قصيرة.
- يوجد زر كتم صوت واضح في إعدادات الطفل/ولي الأمر.

## 6.7 قياس الزمن والدقة
يتم حفظ `wordShownAt` عند ظهور السؤال لأغراض التحليل، لكن **مؤقت الحل الرسمي يبدأ مع أول ضغطة فعلية للطفل** كما هو مطلوب.

المقاييس:
- `firstTapAt`
- `completedAt`
- `activeSolveMs = completedAt - firstTapAt`
- `viewToFirstTapMs = firstTapAt - wordShownAt` كمقياس اختياري لفترة التفكير، ولا يدخل في النقاط.
- `correctTapCount`
- `wrongTapCount`
- `accuracy = correct / (correct + wrong) × 100`
- `hintCount`
- `firstPass = true` إذا اكتملت الكلمة دون خطأ ودون مساعدة.

## 6.8 النقاط
النقاط تشجع الإكمال والدقة ولا تعاقب البطء. صيغة مبدئية قابلة للتعديل:
- 10 نقاط لإكمال الكلمة.
- +2 نقطة لكل حرف صحيح.
- +5 نقاط إذا لم يحدث أي خطأ.
- +3 نقاط إذا لم تستخدم مساعدة.
- لا تخصم نقاط عند الخطأ، ولا يوجد Bonus سرعة في النسخة الأولى.

# 7. اللعبة الثانية: الكلمة والصورة
## 7.1 الهدف
ربط القراءة بالمعنى البصري. يقرأ الطفل كلمة ثم يختار الصورة التي تمثلها.

## 7.2 الوضع الافتراضي المناسب لعمر 5 سنوات
تظهر كلمة واحدة كبيرة في الأعلى، وأسفلها 3 صور كبيرة. واحدة صحيحة وصورتان مشتتتان.
- الضغط الصحيح: إطار أخضر + صوت نجاح ثم انتقال للسؤال التالي.
- الضغط الخاطئ: إطار أحمر قصير + اهتزاز خفيف + صوت تنبيه، وتبقى الخيارات متاحة.
- بعد 3 أخطاء على السؤال نفسه: تضيء الصورة الصحيحة بلطف لمدة قصيرة.
- عند إنهاء مجموعة الأسئلة: احتفال ونقاط.

## 7.3 وضع متقدم لاحقًا
يمكن إضافة شاشة 3 كلمات + 3 صور. يضغط الطفل على كلمة ثم الصورة المطابقة. لا يعتمد السحب والإفلات في النسخة الأولى لأن الضغط أوضح وأسهل على الهواتف الصغيرة.

# 8. نظام المستويات والتدرج
المستوى 1: كلمات 2–3 حروف، مشتتات واضحة، صور مباشرة، 3 مشتتات كحد أقصى.

المستوى 2: كلمات 3 حروف، زيادة التنوع مع بقاء المشتتات سهلة.

المستوى 3: كلمات 4 حروف، 3–4 مشتتات، بدء استخدام بعض الحروف المتشابهة بصريًا.

المستوى 4: كلمات 4–5 حروف، مشتتات أقرب للحروف الصحيحة، وتكرار كلمات أقل ألفة تدريجيًا.

المستوى 5: كلمات 5–6 حروف وكثافة مشتتات أعلى مع المحافظة على وضوح الشاشة.

التقدم التلقائي لا يعتمد على عدد الألعاب فقط. يعتمد على آخر 20 محاولة تقريبًا: دقة جيدة، انخفاض المساعدات، وتحسن الزمن الوسيط. العتبات تكون إعدادات قابلة للتعديل. لا يتم إنزال الطفل تلقائيًا لمستوى أدنى؛ بدلًا من ذلك يمكن تخفيف صعوبة الأسئلة التالية وإظهار توصية لولي الأمر.

# 9. توليد الكلمات بالذكاء الاصطناعي
لا يتم طلب كلمة جديدة من AI لكل سؤال. يوجد بنك كلمات في D1.

سير العمل:
1. مدير النظام يطلب دفعة مثل 20 أو 50 كلمة وفق مستوى وفئة.
2. Workers AI يرجع JSON فقط: الكلمة، الفئة، المستوى المقترح، وهل هي اسم محسوس مناسب للصورة.
3. الـ Worker ينظف الكلمة من التشكيل والتطويل والمسافات.
4. تحقق آلي: حروف عربية فقط، طول مناسب، عدم التكرار، عدم وجود أسماء أشخاص أو ألفاظ غير مناسبة أو كلمات نادرة/غامضة.
5. تحفظ بحالة `pending`.
6. يراجعها مدير النظام ويعتمدها `approved` أو يرفضها.
7. لا تُرسل للطفل إلا الكلمات `approved`.

يفضل بدء المشروع بقائمة أساسية منتقاة يدويًا من كلمات شائعة، ثم استخدام AI للتوسع، وليس الاعتماد على AI وحده منذ أول يوم.

# 10. توليد الصور وتخزينها
للكلمات القابلة للتصوير مثل أسد/قطة/سيارة/باب، ينشئ مدير النظام 2–4 صور بديلة للكلمة عند الحاجة باستخدام نموذج صور متاح في Workers AI وقت التنفيذ.

قواعد Prompt للصور:
- عنصر واحد رئيسي واضح.
- خلفية بسيطة وغير مزدحمة.
- أسلوب طفولي نظيف وثابت الهوية.
- لا توجد كتابة داخل الصورة.
- لا توجد عناصر مخيفة أو غير مناسبة للأطفال.
- الصورة يجب أن تعبّر عن الكلمة مباشرة ولا تعتمد على سياق ثقافي معقد.

بعد التوليد يختار مدير النظام صورة واحدة `selected`، وتخزن الصور في R2. أثناء اللعب لا يتم توليد أي صورة، بل تقرأ الصور مباشرة من R2/CDN وتخزن محليًا عند الإمكان.

صور الأطفال مختلفة: تعتبر بيانات خاصة، وتخزن بمسار خاص ولا تعامل كصور محتوى عامة.

# 11. Local-first والمزامنة قليلة الاتصالات
هذا جزء أساسي من التصميم.

عند بدء جلسة لعب:
- ينفذ العميل طلبًا واحدًا للحصول على حزمة محتوى مناسبة للمستوى، مثل 10–20 سؤالًا.
- تحفظ الحزمة في IndexedDB مع الصور المطلوبة/روابطها.
- كل الضغطات والأزمنة والأخطاء والمساعدات تجمع محليًا في الذاكرة وIndexedDB.
- لا يتم استدعاء Worker عند كل ضغط ولا عند كل سؤال.

عند نهاية الجلسة:
- ينشئ العميل Batch واحدًا يحتوي ملخص الجلسة ومحاولاتها ويرسله إلى `/api/sync`.
- إذا فشل الاتصال، يبقى الـ Batch في `pending_sync` ويعاد إرساله في أول فرصة لاحقة.
- يستخدم `syncBatchId` فريد لمنع تسجيل البيانات مرتين عند إعادة المحاولة.
- كحماية إضافية فقط، إذا كانت الجلسة طويلة جدًا يمكن تنفيذ Checkpoint مجمع كل عدة دقائق أو عند `pagehide`، وليس لكل حدث.

بهذا تستمر اللعبة حتى لو ضعف الإنترنت، ويقل عدد طلبات Workers بصورة كبيرة.

# 12. التحليلات المطلوبة لولي الأمر
لكل طفل بصورة منفصلة:
- عدد جلسات اللعب.
- إجمالي وقت اللعب الفعلي.
- عدد الكلمات التي ظهرت وعدد الكلمات المكتملة.
- الدقة الإجمالية لكل لعبة.
- الزمن الوسيط لإكمال الكلمة، وليس المتوسط فقط.
- اتجاه السرعة أسبوعيًا وشهريًا.
- عدد الأخطاء ونسبة استخدام المساعدة.
- الكلمات الأقوى والكلمات التي تحتاج مراجعة.
- الحروف التي تتكرر الأخطاء عندما تكون هي الحرف المتوقع.
- `firstPass rate` للكلمات المكتملة من أول مرة.
- مستوى الطفل الحالي وتاريخ الانتقال بين المستويات.
- أيام النشاط خلال الفترة.

التقرير الشهري يعرض مقارنة الطفل بنفسه في الفترة السابقة: تحسن الدقة، تغير الزمن الوسيط، كلمات أتقنها، كلمات ما زالت صعبة، وملاحظة عملية بسيطة لولي الأمر.

# 13. تعريف الإتقان والصعوبة
يمكن اعتبار الكلمة «متقنة» إذا ظهرت 3 مرات أو أكثر في أيام/جلسات مختلفة، ودقتها ≥ 90%، واستخدم الطفل مساعدة في ≤ 1 من المحاولات، مع زمن حل مستقر مناسب لمستواه.

يمكن اعتبار الكلمة «تحتاج مراجعة» إذا ظهرت 3 مرات أو أكثر وكانت الدقة < 70% أو استخدمت المساعدة بكثرة أو كان الزمن الوسيط أعلى بكثير من كلمات نفس الطول.

هذه العتبات Config وليست Hard-coded داخل الواجهة.

# 14. نموذج البيانات المقترح في D1
`users`: id, name, email, password_hash, role, created_at, last_login_at.

`children`: id, user_id, display_name, age_or_birth_year, gender_optional, photo_r2_key, local_code, current_level, total_points, created_at, updated_at.

`words`: id, text, normalized_text, category, difficulty_level, is_imageable, source, status, created_at, approved_at.

`word_images`: id, word_id, r2_key, variant_no, status, selected, created_at.

`game_sessions`: id, child_id, game_type, started_at, ended_at, active_ms, points, app_version, client_session_id, sync_batch_id.

`game_rounds`: id, session_id, word_id, round_index, word_shown_at, first_tap_at, completed_at, active_solve_ms, view_to_first_tap_ms, correct_taps, wrong_taps, hint_count, first_pass, points, event_json.

`child_word_stats`: child_id, word_id, exposures, completions, correct_taps, wrong_taps, total_hints, median_solve_ms_or_aggregate_fields, mastery_score, last_seen_at.

`child_level_history`: id, child_id, old_level, new_level, reason, created_at.

`sync_batches`: id, user_id, child_id, client_batch_id, received_at, status. يستخدم لمنع التكرار.

`app_settings` و`content_settings`: إعدادات المستويات، عدد المشتتات، حدود المساعدة، النقاط، التوليد، إلخ.

# 15. واجهات API الأساسية
`POST /api/auth/register`
`POST /api/auth/login`
`POST /api/auth/logout`
`GET /api/me`

`GET /api/children`
`POST /api/children`
`PATCH /api/children/:id`
`POST /api/children/:id/photo`

`GET /api/game-pack?childId=&gameType=&count=` يعيد حزمة أسئلة مناسبة للمستوى فقط.

`POST /api/sync` يستقبل Batch واحدًا للجلسة أو عدة جلسات مؤجلة، ويكون Idempotent باستخدام clientBatchId.

`GET /api/analytics/children/:id?range=30d`
`GET /api/analytics/children/:id/words`

واجهات المدير:
`POST /api/admin/words/generate`
`GET /api/admin/words?status=pending`
`PATCH /api/admin/words/:id/status`
`POST /api/admin/words/:id/images/generate`
`PATCH /api/admin/word-images/:id/select`

# 16. بنية الواجهة الأمامية
React + TypeScript، Mobile-first، ويفضل PWA.

الوحدات:
- Auth.
- Parent Dashboard.
- Child Profiles.
- Child Mode.
- Game Engine مشترك.
- Game: Word Letters.
- Game: Word Image.
- Local Event Store / IndexedDB.
- Sync Manager.
- Analytics UI.
- Admin Content Studio.

يجب بناء Game Engine بحيث تكون كل لعبة Module مستقلة تشترك في: بداية الجلسة، النقاط، الصوت، الاحتفال، التخزين المحلي، والمزامنة. هذا يسمح بإضافة لعبة ثالثة ورابعة من دون إعادة بناء المنصة.

# 17. بنية Cloudflare
- Frontend: Cloudflare Pages.
- API: Cloudflare Workers بـ TypeScript.
- قاعدة البيانات: D1.
- الصور العامة وصور الأطفال: R2 مع فصل منطقي واضح وسياسة وصول مختلفة.
- التوليد: Workers AI، في أدوات الإدارة فقط وليس في مسار اللعب اليومي.
- Cache: Browser/Service Worker + Cloudflare cache للصور والمحتوى العام المناسب.

الهدف هو البقاء ضمن الاستخدام المجاني/المنخفض قدر الإمكان عبر التوليد المسبق، التخزين، الكاش، والـ batch sync. لا تعتمد المنصة على رقم ثابت لحدود الخطة المجانية لأن حدود Cloudflare يمكن أن تتغير.

# 18. الأمان والخصوصية
- كل استعلام طفل يتحقق من ملكية ولي الأمر/الصلاحية على الخادم؛ لا يكفي إخفاء الواجهة.
- كلمات المرور لا تخزن نصًا صريحًا؛ تستخدم خوارزمية Hash موثوقة متوافقة مع بيئة Workers.
- جلسات آمنة عبر HttpOnly/Secure/SameSite، مع Rate limiting لمحاولات الدخول.
- لا توجد صفحات عامة للأطفال ولا بحث عام عن أسمائهم أو أكوادهم.
- جمع أقل قدر ممكن من بيانات الطفل.
- لا تستخدم إعلانات أو تتبع طرف ثالث داخل واجهة الطفل.
- صور الطفل خاصة ولا توضع في نفس سياسة الوصول العامة لصور الكلمات.
- كل Batch مزامنة يحمل معرفًا فريدًا ويطبق Idempotency.

# 19. الشاشات المطلوبة
1. صفحة ترحيب «نقرأ».
2. تسجيل حساب ولي أمر.
3. تسجيل الدخول.
4. لوحة ولي الأمر: بطاقات الأطفال + ملخص.
5. إضافة/تعديل طفل وصورته.
6. اختيار الطفل/إدخال الرقم.
7. الرئيسية الخاصة بالطفل.
8. لعبة حروف الكلمة.
9. لعبة الكلمة والصورة.
10. ملخص جلسة الطفل.
11. تفاصيل طفل في لوحة ولي الأمر.
12. التحليلات الأسبوعية/الشهرية.
13. الإعدادات.
14. لوحة مدير النظام للمحتوى والتوليد والاعتماد.

# 20. التعامل مع الحالات الخاصة في اللعبة الأولى
- حرف مكرر: يجب وجود بطاقة لكل ظهور، والبطاقة المستخدمة لا تستخدم مرة ثانية.
- ضغط حرف صحيح لموضع لاحق قبل دوره: خطأ ترتيب، ولا يحذف من الشاشة.
- ضغط حرف خاطئ ثلاث مرات متتالية: Hint للحرف المطلوب فقط.
- الضغط المتكرر بسرعة على نفس البطاقة المستخدمة: Ignored ولا يسجل كخطأ.
- خروج الطفل من الشاشة ثم العودة: تستعاد الجولة الحالية من الحالة المحلية إن كانت ما تزال صالحة.
- فقد الإنترنت: لا يتوقف اللعب.
- فشل المزامنة: لا تضيع البيانات؛ تبقى في IndexedDB حتى نجاح المزامنة.
- تكرار إرسال Batch: لا يتكرر التسجيل في D1 بسبب idempotency.

# 21. قواعد جودة الكلمات العربية
- تخزين نسخة Display ونسخة Normalized.
- إزالة التشكيل والتطويل من النسخة المستخدمة للعبة.
- المحافظة على الأشكال المهمة مثل أ/إ/آ/ة/ى كحروف مميزة في التعلم، وعدم دمجها عشوائيًا.
- الحروف تقطع إلى وحدات Unicode بعد إزالة العلامات غير المرئية/التشكيل.
- لا تستخدم كلمات فيها شرطة أو رقم أو مسافة في مستويات البداية.
- أسماء الأعلام والألفاظ النادرة والغامضة لا تدخل البنك الافتراضي.
- كلمات لعبة الصور يجب أن تكون محسوسة وقابلة لتمثيل بصري واضح.

# 22. الأداء ومعايير الجودة
- تصميم يعمل من عرض 360px فأعلى.
- لا يحدث Layout shift مزعج أثناء تحميل الصور.
- استجابة الضغط فورية محليًا (<100ms محسوسًا).
- الانتقال بين الأسئلة لا يعتمد على رد API.
- الصور بأحجام محسنة وWebP/AVIF عند الإمكان.
- التطبيق قابل للتثبيت PWA لاحقًا/من البداية.
- الوضع Offline يسمح بإكمال الحزمة المحملة مسبقًا.
- الأخطاء التقنية لا تظهر للطفل كرسائل معقدة.

# 23. إدارة المحتوى
لوحة الإدارة تعرض:
- عدد الكلمات approved/pending/rejected.
- فلترة حسب المستوى والفئة.
- زر «ولد كلمات» مع العدد والمستوى والفئة.
- معاينة الكلمات قبل الاعتماد.
- توليد صور لكلمة واحدة أو دفعة صغيرة.
- عرض 2–4 صور واختيار الصورة الأساسية.
- تعطيل كلمة أو صورة من غير حذف التاريخ الإحصائي.
- منع حذف كلمة مستخدمة في تاريخ اللعب؛ تستخدم حالة archived بدلًا من الحذف.

# 24. ألعاب مستقبلية يجب أن تسمح البنية بإضافتها
- اختر الحرف الأول للكلمة.
- أكمل الحرف الناقص.
- استمع واختر الكلمة (إذا أضيف صوت لاحقًا).
- اقرأ واختر من بين كلمتين/ثلاث.
- مطابقة كلمات متعددة بصور متعددة.
- ترتيب مقاطع أو حروف في مستوى متقدم، لكن ليست جزءًا من MVP الحالي.

# 25. معايير القبول للنسخة الأولى
- يستطيع ولي أمر جديد إنشاء حساب والدخول وإضافة طفلين أو أكثر.
- لكل طفل صورة واسم ومستوى ورقم محلي مستقل.
- لعبة حروف الكلمة تطبق منطق الترتيب والخطأ والمساعدة بعد 3 أخطاء بالضبط.
- مؤقت الحل يبدأ من أول ضغطة، وليس عند عرض الكلمة.
- الأخطاء لا تغير موضع الحرف المتوقع.
- الحرف الصحيح لموضع لاحق إذا ضغط مبكرًا يعتبر خطأ ويبقى صالحًا لاحقًا.
- الحروف المكررة تعمل دون استخدام نفس البطاقة مرتين.
- إكمال الكلمة ينتج مكافأة بصرية وصوتية ونقاطًا.
- لعبة الكلمة والصورة تعمل بثلاث صور على الأقل وتطبق مساعدة بعد 3 أخطاء.
- لا يوجد API call لكل Tap.
- جلسة كاملة يمكن أن تعمل بعد تحميل الحزمة حتى مع انقطاع الشبكة.
- نهاية الجلسة تنتج Batch واحدًا للمزامنة، مع retry وidempotency.
- لوحة ولي الأمر تعرض الوقت، الدقة، السرعة، الأخطاء، المساعدات، الكلمات الصعبة، واتجاه التقدم.
- الكلمات/الصور المولدة بالذكاء الاصطناعي لا تظهر للطفل قبل الاعتماد.
- يمكن لمدير النظام إضافة محتوى جديد من دون Deploy جديد للواجهة.

# 26. مراحل التنفيذ المقترحة
المرحلة 1: الهوية، التسجيل، D1 schema، ملفات الأطفال، وضع الطفل.

المرحلة 2: Game Engine + لعبة حروف الكلمة بكل قواعدها + IndexedDB.

المرحلة 3: Batch sync + التحليلات الأساسية.

المرحلة 4: لعبة الكلمة والصورة + R2 + إدارة الصور.

المرحلة 5: لوحة المحتوى + Workers AI لتوليد الكلمات والصور.

المرحلة 6: تقارير شهرية، ضبط المستويات، PWA/Offline polishing، اختبارات نهائية.

# 27. مطالبة التنفيذ الرئيسية للوكيل البرمجي
ابنِ تطبيق ويب عربي Mobile-first باسم «نقرأ» وفق هذه الوثيقة حرفيًا. استخدم React + TypeScript للواجهة وCloudflare Pages/Workers وD1 وR2 وWorkers AI. نفذ نظام حسابات أولياء الأمور وملفات أطفال متعددة، ودخول طفل مبسط من جهاز موثوق. نفذ Game Engine قابل للتوسع، وابدأ بلعبتين: «حروف الكلمة» و«الكلمة والصورة».

في «حروف الكلمة» يجب أن تظهر الكلمة كاملة بلا تشكيل، وتظهر كل حروفها كبطاقات مستقلة مع مشتتات. الاختيار يجب أن يكون بالترتيب. الضغط الصحيح يثبت البطاقة أخضر ويتقدم موضعًا واحدًا. الضغط على حرف خاطئ أو حرف صحيح لكنه خارج الترتيب لا يغير الموضع المتوقع؛ تومض البطاقة أحمر وتهتز وتصدر صوتًا بسيطًا وتبقى متاحة. بعد 3 أخطاء متتالية للحرف المتوقع أظهر Hint بصريًا خفيفًا على بطاقة صحيحة لمدة قصيرة فقط. عالج الحروف المتكررة كبطاقات مستقلة. ابدأ مؤقت الحل عند أول Tap، وسجل أيضًا وقت عرض الكلمة وفترة ما قبل أول Tap للتحليل من دون إدخالها في النقاط. عند الإكمال اعرض احتفالًا قصيرًا وصوت تصفيق ونقاطًا.

في «الكلمة والصورة» اعرض كلمة واحدة وثلاث صور في النسخة الأولى. الصحيح أخضر ويتقدم، والخاطئ أحمر مؤقتًا، وبعد 3 أخطاء تضيء الصورة الصحيحة. الصور تولد مسبقًا من لوحة المدير وتحفظ في R2؛ لا تولد أثناء اللعب.

اجعل اللعب Local-first: حمّل حزمة أسئلة مرة واحدة، خزّنها في IndexedDB، وسجل التفاعلات محليًا. لا تنفذ أي Worker request لكل Tap أو لكل حرف. في نهاية الجلسة أرسل Batch واحدًا إلى `/api/sync`; احتفظ بأي Batch فاشل محليًا وأعده لاحقًا، مع clientBatchId لمنع التكرار. لا تجعل فشل الشبكة يوقف اللعب.

أنشئ لوحة ولي أمر منفصلة تعرض لكل طفل: عدد الجلسات، وقت اللعب، الدقة، الزمن الوسيط، اتجاه التحسن، الأخطاء، المساعدات، الكلمات المتقنة، الكلمات التي تحتاج مراجعة، والحروف التي يتكرر الخطأ عند انتظارها. لا تعرض Leaderboard بين الأطفال. أنشئ لوحة مدير نظام لإدارة بنك الكلمات، توليد دفعات كلمات عبر Workers AI، التحقق منها، اعتمادها، توليد 2–4 صور لكل كلمة واختيار صورة واحدة.

طبّق الهوية المحددة في هذه الوثيقة، RTL كامل، أزرار كبيرة، خلفية هادئة، نص واضح، animations قصيرة، وأصوات غير مزعجة. طبّق صلاحيات الخادم والأمان والخصوصية، وخزن صور الأطفال كبيانات خاصة. اكتب migrations لـ D1، وأنشئ API typed، واكتب اختبارات Unit لمنطق اللعبة تحديدًا: الترتيب، الحرف المتكرر، الضغط خارج الترتيب، المساعدة بعد 3 أخطاء، تجاهل البطاقة المستخدمة، حساب الزمن والدقة، وidempotent sync. لا تعتبر المهمة مكتملة حتى تنجح معايير القبول في القسم 25.
